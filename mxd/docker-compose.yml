name: mxd
services:
  mxd-runner:
    build: .
    volumes:
      - ./:/project
      - /var/run/docker.sock:/var/run/docker.sock
    extra_hosts:
      - "mxd-control-plane:<insert-docker-gateway-ip>"